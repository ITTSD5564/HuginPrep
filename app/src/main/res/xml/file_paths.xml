<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 项目导出目录：Context.getExternalFilesDir(null)/projects -->
    <external-files-path
        name="projects"
        path="projects/" />
    <!-- 临时缓存（ZIP 打包中转等） -->
    <cache-path
        name="cache"
        path="." />
</paths>
